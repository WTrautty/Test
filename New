bar_description_html = f"""
<div style="margin-top: 15px; padding: 12px; background: #f9f9f9; border-left: 5px solid #2f3542;">
  <p><strong>Revised LEC:</strong> Cramér's V = {lec_cramers_v:.3f}, p = {lec_p:.2e}<br>
  This tells us how strongly the choice of LEC (Local Exchange Carrier) is related to failure. A score of {lec_cramers_v:.2f} means there's a {annotate_strength(lec_cramers_v).replace('(', '').replace(')', '')} connection between which LEC is picked and whether it fails.</p>

  <p><strong>Task Owner:</strong> Cramér's V = {owner_cramers_v:.3f}, p = {owner_p:.2e}<br>
  This shows how connected task owner choice is to failures. A score of {owner_cramers_v:.2f} suggests a {annotate_strength(owner_cramers_v).replace('(', '').replace(')', '')} relationship.</p>

  <p><strong>Monthly Volume:</strong> Pearson r = {volume_corr:.3f}, p = {volume_p:.2e}<br>
  This checks if more orders in a month cause more failures. A score of {volume_corr:.2f} shows a {annotate_strength(volume_corr).replace('(', '').replace(')', '')} link, meaning the amount of work probably doesn’t affect failure much.</p>
</div>
"""
