pie_df = (
    df['Closure Code 1']
    .value_counts(normalize=True)
    .mul(100)
    .reset_index()
    .rename(columns={'index': 'Closure Code', 'Closure Code 1': 'Percentage'})
)
pie = px.pie(pie_df, names='Closure Code', values='Percentage', ...)
